@page
@model IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HotelModel[0].HotelName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HotelModel[0].ImageUrl)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HotelModel[0].Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HotelModel[0].HotelRating)
        </th>
        <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model.HotelModel) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HotelName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ImageUrl)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HotelRating)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.HotelId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.HotelId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.HotelId">Delete</a>
            </td>
        </tr>
    }
    </tbody>
</table>